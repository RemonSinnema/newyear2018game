package name.sinnema.game.engine;


/**
 * A world in a game. For instance, for board games this would be the board. For card games, this would be the
 * respective hands.
 */
public interface World {

  // This is a marker interface. Every game defines its own world(s).

}
